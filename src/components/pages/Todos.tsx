import { InterfaceTodos } from "./TodoPage.page.interface";

const Todos = ({ todos }: InterfaceTodos) => {
  return (
    <div className="todos-collections">
      <h2>Todos</h2>
    </div>
  );
};

export default Todos;
